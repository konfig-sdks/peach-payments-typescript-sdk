import { PeachPayments } from "./index";

describe("peach-payments-typescript-sdk", () => {
    it("initialize client", async () => {
        const peachpayments = new PeachPayments({
        });
    });
});
